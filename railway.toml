[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"

[deploy]
startCommand = "gunicorn core.wsgi:application --bind 0.0.0.0:${PORT:-8000}"
healthcheckPath = "/api/countries/status/"
healthcheckTimeout = 30